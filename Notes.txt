*** EF COMMANDS ***

dotnet ef migrations list --context MainContext

dotnet ef migrations add mig1 --context MainContext
dotnet ef database update --context MainContext

dotnet ef migrations add mig2 --context ServiceLogContext
dotnet ef database update --context ServiceLogContext

*** TODO ***

Auto-detect user from localSettings
Change password

	var $element = $("#coachSelect"); // the element that Select2 is initialized on

	var $option = $("<option selected></option>"); // the new base option
	$option.val("fug"); // set the id
	$option.text("fug"); // set the text

	$element.append($option); // add it to the list of selections
	$element.trigger("change"); // tell Select2 to update